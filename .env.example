# Copy to .env.local for local overrides (gitignored by Vite).
# VITE_* variables are inlined into the client bundle at build time.
# Do NOT put secrets here — VITE_* vars ship to the browser.

# Google Analytics 4 Measurement ID. Leave empty to disable analytics.
VITE_GA_ID=
